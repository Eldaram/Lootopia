module.exports = {
  up: function (knex) {
    return knex.schema.createTable('table_name', function (table) {
      table.increments();  // Ajoute une clé primaire incrémentée
      table.string('column_name');  // Ajoute une colonne de type chaîne
    });
  },
  down: function (knex) {
    return knex.schema.dropTable('table_name');  // Supprime la table lors du rollback
  }
};
